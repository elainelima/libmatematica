#include <math.h>//biblioteca com as função de matematicas que serão aplicada no codigo
#include "matematica.h"//incluindo o arquivo de cabecalho criado

float calcular_seno(float angulo_radianos){
    return sin(angulo_radianos);//utiliza a função sin da biblioteca math para calcular o seno do angulo definido na variavel de entrada angulo_radiano
}

float calcular_cosseno(float angulo_radianos){
    return cos(angulo_radianos);//utiliza a função cos da biblioteca math para calcular o seno do angulo definido na variavel de entrada angulo_radiano

}

float calcular_tangente(float angulo_radianos){
    return tan(angulo_radianos);//utiliza a função tan da biblioteca math para calcular o seno do angulo definido na variavel de entrada angulo_radiano

}

/////